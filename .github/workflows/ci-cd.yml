name: CI/CD - Test, Build & Deploy

# CI: se ejecuta en cada push/PR a main para validar que nada rompe.
# CD: el deploy solo se dispara en push a main y solo si CI pasó.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

jobs:
  # -------------------------------------------------------------------
  # 1) CI: pruebas de humo del backend
  # -------------------------------------------------------------------
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r api/requirements.txt
          pip install pytest httpx pandas numpy

      - name: "Run smoke tests (API)"
        run: pytest tests/test_api.py -v

      - name: "Run retrain pipeline tests"
        run: pytest tests/test_retrain.py -v --tb=short

  # -------------------------------------------------------------------
  # 2) CI: valida que ambas imágenes Docker construyen sin errores
  # -------------------------------------------------------------------
  build:
    name: Build Docker images
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Build API image
        run: docker build -t pm-api:ci ./api

      - name: Build App (Streamlit) image
        run: docker build -t pm-app:ci ./app

  # -------------------------------------------------------------------
  # 3) CD: dispara el redeploy en Render (solo push a main, no en PRs)
  # -------------------------------------------------------------------
  deploy:
    name: Trigger deploy on Render
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Deploy pm-api
        run: curl -fsSL -X POST "${{ secrets.RENDER_DEPLOY_HOOK_API }}"

      - name: Deploy pm-app
        run: curl -fsSL -X POST "${{ secrets.RENDER_DEPLOY_HOOK_APP }}"